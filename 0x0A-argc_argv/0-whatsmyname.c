#include "main.h"

/**
 * main - gets the name of the programme
 * @i: set the index
 * Returin: 0
 */
int main(int argc __attribute__((unsused)), char *argv[])
{
	printf("%s\n", *argv);
	return (0);
}
