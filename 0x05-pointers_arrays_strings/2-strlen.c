#include "main.h"

/**
 * _strlen - returns the length of a string
 * @s: string to evaluate
 * len: convertd
 *
 * Return: the length of the string
 */
int _strlen(char *s)
{
	int i;

	int len;

	s = "hello world";
	len = _strlen(s);

	return (len);

}
