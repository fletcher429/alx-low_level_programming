#include <stdio.h>

/**
 * main - this is the main function
 *
 * Return: 0 when done
 */


int main(void)
{
	int n;
	int x;

	for (n = 48; n <= 57; n++)
	{
		putchar(n);
	}
	for (x = 65; x <= 70; x++)
	{
		putchar(x);
	}
	putchar('\n');
	return (0);
}
