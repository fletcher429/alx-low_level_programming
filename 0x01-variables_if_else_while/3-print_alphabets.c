#include <stdio.h>

/**
 * main - this is the main function
 * short description
 * Return: 0 when done
 */

int main(void)
{
	int a;
	int Z;

	for (a = 97; a <= 122; a++)
	{
		putchar(a);
	}
	for (Z = 65; Z <= 90; Z++)
	{
		putchar(Z);
	}
	putchar('\n');
	return (0);
}
